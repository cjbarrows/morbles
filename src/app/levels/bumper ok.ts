import { GameLevel } from '../gameLevel';

const level: GameLevel = {
  name: 'Bumper OK',
  rows: 2,
  columns: 2,
  startingBalls: 'RG',
  endingBalls: 'RG',
  map: '  ' + 'R ',
};

export default level;
